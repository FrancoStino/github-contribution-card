{
  "name": "github-contribution-card",
  "version": "1.1.1",
  "description": "Generate SVG cards showing GitHub contribution statistics",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsx scripts/gen-demo.ts && esbuild src/server.ts --bundle --platform=node --format=cjs --outfile=server.js --packages=external --loader:.html=text --loader:.css=text --loader:.script=text --footer:js=\"module.exports = app;\"",
    "build:action": "esbuild action/src/index.ts --bundle --platform=node --format=cjs --outfile=action/dist/index.js",
    "vercel-build": "rm -f server.js && yarn build",
    "start": "yarn build && node server.js",
    "dev": "tsx watch src/server.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FrancoStino/github-contribution-card.git"
  },
  "author": "FrancoStino",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/FrancoStino/github-contribution-card/issues"
  },
  "homepage": "https://github.com/FrancoStino/github-contribution-card#readme",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "axios": "^1.18.1",
    "compression": "^1.7.4",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "lodash": "^4.17.21",
    "lru-cache": "^11.5.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "eslint": "10.6.0",
    "prettier": "^3.9.5",
    "tsc-alias": "^1.8.17",
    "tsx": "^4.20.0",
    "typescript": "^7.0.2"
  }
}
